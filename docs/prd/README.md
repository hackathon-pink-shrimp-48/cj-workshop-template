# docs/prd — PRD 저장소

로컬 작업(클라우드 위임 없이)의 **중간 산출물** 저장소입니다.
`to-prd` 스킬이 정렬된 요청을 여기에 PRD로 씁니다.

```
docs/prd/<기능-슬러그>.md
```

각 PRD는 원래 채팅 없이도 이해되도록: 문제·해결·범위(포함/제외)·시나리오(Given-When-Then)·요구사항(FR/NFR)·완료조건(`bash gate.sh` 통과 포함)·오픈이슈.
